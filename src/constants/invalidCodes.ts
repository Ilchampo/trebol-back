const invalidCodes = {
    INVALID_DATA: 'invalid-data',
    INVALID_ID: 'invalid-id',
    INVALID_TYPE: 'invalid-type',
    INVALID_NOT_FOUND: 'not-found',
    SOMETHING_BROKEN: 'something-broken',
} as const;

export default invalidCodes;
